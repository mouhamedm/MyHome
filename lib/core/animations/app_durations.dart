class AppDurations {
  AppDurations._();

  static const Duration fast = Duration(milliseconds: 180);
  static const Duration micro = Duration(milliseconds: 220);
  static const Duration normal = Duration(milliseconds: 320);
  static const Duration uiTransition = Duration(milliseconds: 380);
  static const Duration page = Duration(milliseconds: 450);
  static const Duration staggeredDelay = Duration(milliseconds: 60);
}
